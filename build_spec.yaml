version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

steps:
  - type: Command
    name: "Start Build"
    command: |
      echo "OCI DevOps build started"

  - type: Command
    name: "List Files"
    command: |
      ls -la

outputArtifacts:
  - name: build_output
    type: BINARY
    location: .
