version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

steps:
  - type: Command
    name: "Check Files"
    command: |
      echo "OCI DevOps build started"
      pwd
      ls -la

outputArtifacts:
  - name: build_output
    type: BINARY
    location: .
